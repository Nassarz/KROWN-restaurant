// Database seeding is now handled automatically by dataStore.ts via Supabase.
// On first launch, if all tables are empty, dataStore.seedAll() runs automatically.
export async function seedFirestoreDatabase() {
  return 0;
}
